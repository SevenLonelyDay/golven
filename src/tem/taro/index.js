const {
    actionTem
} = require('./action');
const {
    componentTem
} = require('./component');
const {
    configTem
} = require('./config');
const {
    constantTem
} = require('./constant');
const {
    interfaceTem
} = require('./interface');
const {
    pageTem
} = require('./page');
const {
    reducerTem
} = require('./reducer');
const {
    scssTem
} = require('./scss');
module.exports = {
    actionTem,
    componentTem,
    configTem,
    constantTem,
    interfaceTem,
    pageTem,
    reducerTem,
    scssTem,
}