const {
    shellForm
} = require('./shellForm');
const {
    firstToUpperCase,
    checkDir,
    mkFile,
    appendWriteFile,
    mkDir,
    log,
    argv
} = require('./util');


module.exports = {
    shellForm,
    firstToUpperCase,
    checkDir,
    mkFile,
    appendWriteFile,
    mkDir,
    log,
    argv
}