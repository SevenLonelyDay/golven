const {
    form
} = require('./form');
const {
    firstToUpperCase,
    checkDir,
    mkFile,
    appendWriteFile,
    mkDir,
    log
} = require('./util');


module.exports = {
    form,
    firstToUpperCase,
    checkDir,
    mkFile,
    appendWriteFile,
    mkDir,
    log
}